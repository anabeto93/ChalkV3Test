{% extends ':Admin/Form:base_form_layout.html.twig' %}

{% block title 'admin.session.update.title'|trans({'%sessionTitle%': session.title, '%courseTitle%': course.title }) %}

{% block form_title 'admin.session.update.title'|trans({'%sessionTitle%': session.title, '%courseTitle%': course.title }) %}

{% block goback_link path('admin_session_list', {'course': course.id}) %}
