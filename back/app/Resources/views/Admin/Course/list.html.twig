{% extends '::base.html.twig' %}

{% block title 'admin.course.title'|trans %}

{% block content %}
    <h1>
        {{ 'admin.course.title'|trans }}
        <span class="pull-right">
            <a class="btn btn-primary" href="{{ path('admin_course_create') }}">
                {{ 'admin.course.create.title'|trans }}
            </a>
        </span>
    </h1>
    <hr>
    {% if courses is empty %}
        <div class="alert alert-info">{{ 'admin.course.no-result'|trans }}</div>
    {% else %}
        <table class="table table-striped table-condensed">
            <thead>
            <tr>
                <th class="col-md-2">{{ 'admin.course.column.title'|trans }}</th>
                <th class="col-md-2">{{ 'admin.course.column.teacherName'|trans }}</th>
                <th class="col-md-2">{{ 'admin.course.column.university'|trans }}</th>
                <th class="col-md-1">{{ 'admin.course.column.folders'|trans }}</th>
                <th class="col-md-1">{{ 'admin.course.column.sessions'|trans }}</th>
                <th class="col-md-1">{{ 'admin.course.column.students'|trans }}</th>
                <th class="col-md-1">{{ 'admin.course.column.enabled'|trans }}</th>
                <th class="col-md-1"></th>
            </tr>
            </thead>
            <tbody>
            {% for course in courses %}
                <tr>
                    <td>{{ course.title }}</td>
                    <td>{{ course.teacherName }}</td>
                    <td>{{ course.university }}</td>
                    <td>
                        <a href="{{ path('admin_folder_list', {'course': course.id}) }}">
                            {{ course.numberOfFolders }}
                        </a>
                    </td>
                    <td>
                        <a href="{{ path('admin_session_list', {'course': course.id}) }}">
                            {{ course.numberOfSessions }}
                        </a>
                    </td>
                    <td>
                        <a href="{{ path('admin_course_assign_user', {'course': course.id}) }}">
                            {{ course.numberOfStudents }}
                        </a>
                    </td>
                    <td>{{ course.enabled ? '✔︎' : '' }}</td>
                    <td class="text-right">
                        <a href="{{ path('admin_course_update', {'course': course.id}) }}" class="btn btn-sm btn-default">
                            {{ 'admin.course.action.update'|trans }}
                        </a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% endif %}
{% endblock %}
