Query:
    type: object
    config:
        description: "Chalkboard Education API"
        fields:
            user:
                type: "User!"
                resolve: "@=resolver('user')"
            course:
                type: "Course"
                args:
                    uuid:
                        description: "The uuid of the course"
                        type: "String!"
                resolve: "@=resolver('course', [args])"
            courses:
                type: "[Course]"
                resolve: "@=resolver('courses', [args])"

